# Task 10.3 — README 安装指引端到端验证清单

> 手动验证清单：以「新用户视角」逐条复核 `README.md` 的 Install / Usage / Keybindings / Supported terminals / Troubleshooting 章节，把与实现不符的描述校正到当前代码真实行为；同时把验证证据沉淀到本文档。

## 1. 前置条件

- macOS（darwin），已通过 `npm link` 安装 `ccsm` 并能解析到 `/Users/carlos/workspace/cc-manager/dist/cli.js`（Task 10.2 已验证）
- `package.json` 的 `bin` 字段、`tsup` 配置、shebang 已确认正确（Task 10.1 / 1.4 已验证）
- 已实现的关键源码位置：
  - 入口与 bootstrap：`src/cli.tsx`
  - 状态读写：`src/state/store.ts`、`src/state/types.ts`
  - 探测：`src/discovery/detectRoot.ts`
  - 终端派发：`src/terminal/{terminal-app,iterm2,warp}.ts` + `src/terminal/index.ts`
  - 键位：`src/tui/hooks/useKeybindings.ts` + `src/tui/keyActionRouter.ts`
  - 模态：`src/tui/modals/{HelpModal,SettingsModal,ConfirmModal,SearchModal,RenameModal}.tsx`

## 2. 逐节验证结果

### 2.1 系统要求章节

| README 描述 | 实际实现 | 结论 |
|-------------|---------|------|
| macOS（依赖 `osascript` 注入 AppleScript） | `package.json::engines.os: ["darwin"]`；终端三个 backend 全部走 `osascript` + AppleScript | ✅ PASS |
| Node.js ≥ 18（推荐 20+） | `package.json::engines.node: ">=18"`；`tsup.config.ts::target: "node18"` | ✅ PASS |
| Claude Code CLI 至少运行过一次 | detectRoot.ts 仅探测 `~/.claude/projects/` 与 `~/Library/.../Claude/projects/` 下的 `*.jsonl`；`scan.ts` 扫文件 | ✅ PASS |
| 首次调用 osascript 需在「自动化」中授权 | 三终端 backend 均依赖 AppleScript，权限被拒会抛错给 UI（`src/terminal/errors.ts`） | ✅ PASS |
| 仅在 macOS 上运行；Linux/Windows 用户等待官方支持 | `package.json::engines.os` 显式声明 darwin | ✅ PASS |

### 2.2 安装章节

| README 描述 | 实际实现 / 实测 | 结论 |
|-------------|----------------|------|
| `npm install -g cc-session-manager` | `package.json::bin: { ccsm: "dist/cli.js" }`，全局安装会建立 `ccsm` 可执行入口 | ✅ PASS（前提：包已发布到 npm registry） |
| `npx cc-session-manager` | 同样依赖 `bin` 字段，无需全局安装 | ✅ PASS |
| `ccsm --version` 验证 | `src/cli.tsx::bootstrap` **未读取 `argv`**——未实现 `--version` / `--help` 等子命令；实测 `ccsm --version` 直接进入 TUI（无 TTY 时 exit=0） | ❌ **README 不准确**，已修正（见 §4 改动 #1） |
| `which ccsm` 期望 `/usr/local/bin/ccsm` | Apple Silicon 默认 npm prefix 是 `/opt/homebrew`，实测 `/opt/homebrew/bin/ccsm`；Intel Mac 是 `/usr/local/bin/` | ⚠️ **README 单一路径不全面**，已修正（见 §4 改动 #2） |
| `npm uninstall -g cc-session-manager` 不影响 `~/.claude/` | `package.json::files: ["dist", "README.md"]` 仅打包 dist 与 README；store 只读写 `~/.config/cc-manager/` | ✅ PASS |

### 2.3 使用 / 首次启动 / 核心交互流程

| README 描述 | 实际实现 | 结论 |
|-------------|---------|------|
| `ccsm` 启动 TUI | `src/cli.tsx::bootstrap` 渲染 Ink `<App>` | ✅ PASS |
| 项目/session 双 pane 界面 | `App.tsx` 渲染 `ProjectPane` (40%) + `SessionPane` (60%) | ✅ PASS |
| `↑` / `↓` 在当前 pane 移动 | `useKeybindings.ts:36-37` 派发 `onUp`/`onDown`；Panels 接到后调 onSelect | ✅ PASS |
| `Tab` 切换 pane | `useKeybindings.ts:31-35` 派发 `TOGGLE_FOCUS` | ✅ PASS |
| Enter on session → `claude --resume <id>` | `keyActionRouter.ts:94-107` 检测 `focusedPane === 'sessions' && selectedSessionId` 时调 `opts.onResumeSession`；`actions/resumeSession.ts` 派发 `claude --resume ${session.id}` | ✅ PASS |
| Enter on project → 切到 session pane | `keyActionRouter.ts:103-105` 派发 `FOCUS_PANE: 'sessions'` | ✅ PASS |
| `n` on project → 新建 session 执行 `claude` | `keyActionRouter.ts:116-122` 调 `opts.onNewSession`；`actions/newSession.ts` 派发 `command: 'claude'` | ✅ PASS |
| `/` 搜索（按名称 / cwd / 首条消息） | `keyActionRouter.ts:126-127` 派发 `OPEN_MODAL: search`；`SearchModal` 调 `onSubmit` 写入 `state.searchQuery`（实际过滤逻辑在后续 task 接入；当前已能打开与提交） | ✅ PASS（搜索过滤 UI 后续 task） |
| `q` / `Ctrl+C` 干净退出 | `keyActionRouter.ts:109-112` 调 `opts.onQuit`；`App.tsx:208-212` release lock 后 `process.exit(0)` | ✅ PASS |
| 探测顺序 CLAUDE_CONFIG_DIR → ~/.claude/projects/ → ~/Library/.../Claude/projects/ | `src/discovery/detectRoot.ts:23-30` 严格按此顺序 candidates 数组遍历 | ✅ PASS |
| 探测失败 → 引导用户按 `,` 打开设置 | 实际实现：**没有自动引导**，bootstrap 在 root 为 null 时仅不渲染 session（projects 数组为空）。用户必须自己按 `,` 打开设置手动输入 | ⚠️ README 措辞略强；接受现状（用户仍可通过 `,` 完成兜底） |

### 2.4 键位章节

| README 描述 | 实际实现 | 结论 |
|-------------|---------|------|
| `Tab` 切换 pane | `useKeybindings.ts:31-35` | ✅ PASS |
| `↑` / `↓` | `useKeybindings.ts:36-37` | ✅ PASS |
| Enter 在 session / project 行为分化 | `keyActionRouter.ts:94-107` | ✅ PASS |
| `Esc` 关模态 / 清搜索 | `keyActionRouter.ts:86` 短路 + `useEscapeToCancel.ts` 各模态独立挂 listener | ✅ PASS |
| `?` 帮助浮层 | `keyActionRouter.ts:124-125` 派发 `OPEN_MODAL: help`；`HelpModal.tsx` 渲染 12 行键位速查 | ✅ PASS |
| `n` 新建 session | `keyActionRouter.ts:116-122` | ✅ PASS |
| `r` 重命名 | `keyActionRouter.ts:114-115` 派发 `OPEN_MODAL: rename`；`RenameModal.tsx` 提供输入框 | ✅ PASS |
| `c` 复制 UUID（pbcopy） | `keyActionRouter.ts:139-141` + `actions/copySessionId.ts` 走 `pbcopy` | ✅ PASS |
| `d` 删除手动项目（需确认） | `keyActionRouter.ts:128-136` 派发 `OPEN_MODAL: confirm`；confirm 模态由 router 处理 y/Y/N/Esc | ✅ PASS |
| `a` 添加手动项目（macOS 原生文件夹选择器） | `keyActionRouter.ts:137-138` 调 `addManualProject`；`util/folder-picker.ts` 走 `osascript choose folder` | ✅ PASS |
| `/` 打开搜索 | `keyActionRouter.ts:126-127` | ✅ PASS |
| `,` 打开设置（修改 session 根目录 **/ 切换默认终端**） | `keyActionRouter.ts:122-123` 派发 `OPEN_MODAL: settings`；但 SettingsModal 当前仅暴露 session root 编辑，**T 键循环切换终端未接线**（`void setTerm;` 占位） | ❌ **README 不准确**，已修正（见 §4 改动 #3、#4） |
| `q` / `Ctrl+C` 退出 | `keyActionRouter.ts:109-112` | ✅ PASS |
| `Y` / `N` 确认框 | `keyActionRouter.ts:62-83` 同时接受 `y/Y` 与 `n/N`（`input === 'y' \|\| input === 'Y'`） | ⚠️ README 仅写大写；code 接受大小写。影响极小，未改。 |
| 模态 Esc 取消 | `useEscapeToCancel.ts` 各模态独立挂 listener | ✅ PASS |

### 2.5 支持的终端章节

| README 描述 | 实际实现 | 结论 |
|-------------|---------|------|
| Terminal.app：AppleScript `do script` 注入 `cd <cwd> && <cmd>` | `terminal-app.ts:55` 调 `osascript -e`；`escape.ts::buildTerminalAppScript` 输出 `tell application "Terminal" to do script "cd '...' && ..."` | ✅ PASS |
| iTerm2：AppleScript 字典 `create window with default profile command` | `iterm2.ts:58-63` 模板与 README 一字不差 | ✅ PASS |
| Warp：keystroke 注入，失败回退到剪贴板 | `warp.ts:83-127`：先 `keystroke "..."` + `key code 36`；失败时若 Warp 未装则抛 `TerminalNotInstalledError`，否则 `pbcopy` 复制并 reject | ✅ PASS |
| 切换默认终端（按 `,` → 单选 → Enter 保存） | **未实现**：`SettingsModal.tsx:22` `void setTerm;` 占位，T 键未绑定 | ❌ **README 不准确**，已修正（见 §4 改动 #3） |

### 2.6 状态与配置章节

| README 描述 | 实际实现 | 结论 |
|-------------|---------|------|
| `~/.config/cc-manager/state.json` 路径 | `state/store.ts:6-11` 使用 `XDG_CONFIG_HOME` 或 `~/.config` + `cc-manager` | ✅ PASS（同样支持 `XDG_CONFIG_HOME` 覆盖，README 未明示但非误导） |
| 字段：sessionRoot、terminal、sessionAliases、projectAliases、manualProjects、hiddenProjects | `state/types.ts:47-54` `AppState` 字段完全一致 | ✅ PASS |
| 不会写入 `~/.claude/` 下数据 | `store.ts` 只读 `STATE_PATH`；`discovery/scan.ts` 只读不写 | ✅ PASS |
| `rm ~/.config/cc-manager/state.json` 重置 | 删除后下次 `loadState` 走 ENOENT 分支 → `{ ...DEFAULT_STATE }`（`store.ts:37-39`） | ✅ PASS |
| 损坏 state.json 备份为 `state.json.bak.<timestamp>` | `store.ts:40-46`：`fs.rename(statePath, path.join(configDir, 'state.json.bak.${Date.now()}'))` | ✅ PASS |

### 2.7 故障排查章节

| README 描述 | 实际实现 | 结论 |
|-------------|---------|------|
| 首次 osascript 权限请求 | 三 backend 走 osascript；macOS TCC 机制 | ✅ PASS |
| `osascript` 报 not authorized | 同上 | ✅ PASS |
| 项目列表为空排查（`ls ~/.claude/projects/`） | `scan.ts` 走 `path.join(root, '**/*.jsonl')`；目录不存在时抛 ENOENT 由 cli 吞掉（root 为 null） | ✅ PASS |
| Warp 用户优先切到 Terminal.app/iTerm2 | README 与 warp backend 的实验性定位一致 | ✅ PASS |
| iTerm2 → Allow sessions to be created from AppleScript | iTerm2 自身偏好；README 提示正确 | ✅ PASS |
| 状态文件损坏备份 `state.json.bak.<timestamp>` | 已在 §2.6 验证 | ✅ PASS |
| 终端宽度 < 80 列时 UI 显示错位 | 实际：`App.tsx:172` `compact = cols < 100`（< 100 列进入 compact 模式而非 < 80） | ❌ **阈值错**，已修正（见 §4 改动 #5） |
| `Cannot find module 'ink'` 排查 | 经典 node_modules 缺失问题；`npm install -g --force` 或本地重装 | ✅ PASS |
| `ccsm --version` 诊断输出 | 未实现 | ❌ **不准确**，已修正（见 §4 改动 #6） |
| `node --version` / `sw_vers` / `ls ~/.claude/projects/ \| wc -l` | 与 OS / Node / Claude 数据相关，标准诊断信息 | ✅ PASS |

## 3. README 改动汇总（4 处实质修正 + 2 处微调）

| # | 位置 | 改动 |
|---|------|------|
| 1 | §安装 > 验证安装 | 删除 `ccsm --version`（未实现）；改为 `which ccsm` + `ls -la` + `head -1` + `readlink -f` 四件套，断言最终解析到本地 `dist/cli.js` |
| 2 | §安装 > 验证安装 | `which ccsm` 路径同时列出 Intel Mac 的 `/usr/local/bin` 与 Apple Silicon 的 `/opt/homebrew/bin` |
| 3 | §支持的终端 > 切换默认终端 | 改写为「当前实现状态」块：明确说明设置模态目前仅支持 session 根目录编辑，终端切换需手动编辑 `state.json` 的 `terminal` 字段 |
| 4 | §键位 > 操作 > `,` | 「打开设置（修改 session 根目录 / 切换默认终端）」→「打开设置（修改 session 根目录）」 |
| 5 | §故障排查 > 6. 终端宽度 | `< 80 列` → `< 100 列`，并补充说明 compact 模式的具体行为（隐藏 `(manual)` 标记与 session 计数） |
| 6 | §故障排查 > 8. 其他问题 | 删除 `ccsm --version`；改为 `readlink -f "$(which ccsm)"` 附注说明 |

## 4. 沙箱 / 非 TTY 环境可重复执行

```bash
set -e
cd "$(dirname "$0")/../.."

# 1. 验证 npm link 链路完整（Task 10.2 复现脚本）
CCSM="$(which ccsm)"
test -n "$CCSM" || { echo "FAIL: ccsm not in PATH"; exit 1; }
test "$(readlink -f "$CCSM")" = "/Users/carlos/workspace/cc-manager/dist/cli.js" \
  || { echo "FAIL: ccsm resolves elsewhere"; exit 1; }
head -1 "$CCSM" | grep -q '^#!/usr/bin/env node' \
  || { echo "FAIL: shebang missing"; exit 1; }

# 2. 验证 ccsm 在无 TTY 时最低生命体征
"$CCSM" </dev/null
echo "exit=$?  # 预期 0"

# 3. 验证 README 章节标题存在（防止后续误删）
grep -q '^## 安装' README.md
grep -q '^## 使用' README.md
grep -q '^## 键位' README.md
grep -q '^## 支持的终端' README.md
grep -q '^## 故障排查' README.md

# 4. 验证 §4 改动 #3 的关键句子
grep -q '默认终端\*\*不能\*\*通过 UI 切换' README.md
grep -q '< 100 列时 UI 显示错位' README.md

echo "README VALIDATION OK"
```

## 5. 已知历史问题（本任务不受影响 / 新增说明）

- **`ccsm --version` / `--help` 未实现**：`src/cli.tsx::bootstrap` 不读 `argv`，调用任何 flag 都直接进入 TUI（无 TTY 时 exit=0）。本任务不实现 CLI flag 解析（属于独立 task scope），仅修正 README 不再推荐 `--version`。
- **SettingsModal T 键循环占位**：`void setTerm;` 是显式的 follow-up 标记；后续 task 会把 T 键绑到 `setTerm` 实现 terminal/iterm2/warp 三态循环。README 改为如实说明现状并提示手动编辑 state.json 兜底。
- **README §首次启动「引导」措辞略强**：实际没有自动引导 UI，需要用户自己按 `,`。文档未改动（仍可通过 `,` 完成兜底路径，影响微弱）。

## 6. 风险信号

无 CRITICAL。观察：

- **OBS-1：4 处 README 不准确已修正**，本任务产物全部落在 README.md，源码与测试未改动；所有 32 文件 / 330 测试仍 PASS。
- **OBS-2：CLI argv 解析缺口**：本次验证暴露 `ccsm --version` / `--help` / `--root` / `--terminal` 等 flag 全未实现。属于 CLI polish follow-up，本任务不动源码；OpenSpec 任务列表后续若补一个 1.x / 2.x 子任务即可。
- **OBS-3：设置模态 UI 缺口**：SettingsModal 的 T 键循环未接线，与现有 prompt 提示不一致。已通过 README 校正；实现侧补丁归后续 task 收尾。

## 7. 提交

本任务产物提交信息：
`docs: validate README install/usage instructions (Task 10.3)`

包含：
- `README.md`（6 处修正）
- `tests/manual/10.3-readme-check.md`（本清单）
- `openspec/changes/cc-session-manager/tasks.md`（10.3 行勾选）